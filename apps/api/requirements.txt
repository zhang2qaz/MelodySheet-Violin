fastapi>=0.115.0,<0.129.0
uvicorn[standard]>=0.30.0,<0.40.0
python-multipart>=0.0.9
pydantic>=2.7.0
music21>=8.3.0,<9.0.0
basic-pitch>=0.4.0
scikit-learn>=1.3.0,<1.6.0
librosa>=0.10.1
soundfile>=0.12.1
numpy>=1.24.0
scipy>=1.10.0
matplotlib>=3.7.0
# Demucs source separation (htdemucs_6s) -- isolates the "other" stem
# where bowed strings land, before transcription. Default-on. Pulls in
# PyTorch (~700 MB) so the installer grows substantially; users with
# accompanied recordings see the biggest accuracy win.
demucs>=4.0.0
pytest>=8.2.0
httpx>=0.27.0

# Optional, recommended for production-quality results — install separately if your
# deployment can handle the extra disk and runtime cost:
#   pip install demucs            # htdemucs_6s six-stem source separation
#   pip install tensorflow-hub    # YAMNet instrument identification
#   pip install crepe             # CREPE deep pitch tracker (monophonic transcription)
